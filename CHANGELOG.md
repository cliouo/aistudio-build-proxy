# Changelog

## v0.1.0
- Initial restructuring to agent/server architecture.
- Added browser driver and Docker setup.

## v0.2.0
- Support endpoint discovery via `/tmp/endpoint.txt`.
- Rename `FRONTEND_URL` env to `BROWSER_URL` and add bootstrap login scripts.
- Provide Windows PowerShell helpers.
